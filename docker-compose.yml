services:
  opcua-sim-web:
    build: .
    image: opcua-sim-web:2.0
    container_name: opcua-sim-web
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./static:/app/static:ro
